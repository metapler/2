# Private key for mining wallet (64 character hex string without 0x prefix)
PRIVATE_KEY=your_private_key_here

# Optional: Custom RPC URL (replace the default in miner.js if needed)
# RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your-api-key
